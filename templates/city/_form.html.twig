{{ form_start(form) }}
{{ form_widget(form, { 'attr': {'class': 'input-name'} }) }}
<br>
<div style="display : flex; align-items : center; gap : 2rem; margin-left : auto; margin-right : auto; max-width : 220px">
	<a href="{{ path('app_city_index') }}">&#8592; Retour</a>
	<button class="btn">{{ button_label|default('Enregistrer') }}</button>
</div>

{{ form_end(form) }}
