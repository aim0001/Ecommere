{% extends 'base.html.twig' %}

{% block title %}Sous Category{% endblock %}

{% block body %}
<main class="Block">
    <span class="retour">
        <a href="{{ path('app_sub_category_index') }}"class="lien-retour">&#8592; Retour</a>
    </span>
    <br/>
    <h4>Sous Categorie</h4>
    <br>
    <div class="Block-btn-show">
        <a href="{{ path('app_sub_category_edit', {'id': sub_category.id}) }}"class="btn-edit">Modifier</a>

        {{ include('sub_category/_delete_form.html.twig') }}
    </div>
    <br/>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sub_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sub_category.name }}</td>
            </tr>
            <tr>
                <th>Catégorie</th>
                <td>{{ sub_category.categorie.name }}</td>
            </tr>
        </tbody>
    </table>
</main>
{% endblock %}
